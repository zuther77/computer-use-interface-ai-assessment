"""Per-run evidence folder conventions (Phase 9; DECISIONS.md §10b).

One self-contained subfolder per run — discovery_run_{id}/,
replay_success_{id}/, replay_error_{id}/ — plus the shared canonical
artifact store.
"""
