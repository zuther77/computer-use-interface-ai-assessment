"""Two-dimensional allowlist enforcement (Phase 3; DECISIONS.md §8a).

Domain/route allowlist checked at ``navigate`` plus an action-type
capability permission set checked at every tool invocation, loaded from
/config/allowlist.yaml.
"""
